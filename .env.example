# ===========================================
# ENVIRONMENT CONFIGURATION
# ===========================================
# Supported environments: development, uat, production
#
# All environments use AWS infrastructure:
# - development: AWS dev server + RDS, .env file OK
# - uat: AWS UAT environment, use Secrets Manager
# - production: AWS Production, use Secrets Manager
#
# Development Setup:
# - Code syncs from local machine to AWS dev server
# - Dev server connects to development RDS instance
#
# NEVER commit real credentials to version control.
# ===========================================

# Application
# Options: development | uat | production
NODE_ENV=development
PORT=3000

# Database (AWS RDS for all environments)
DB_HOST=domain-monitor-dev.xxxxx.us-east-1.rds.amazonaws.com
DB_USER=admin
DB_PASSWORD=your_dev_rds_password
DB_NAME=domain_monitor_dev
DB_PORT=3306

# JWT Authentication
JWT_SECRET=dev-only-change-in-production-use-secrets-manager
# JWT_ACCESS_EXPIRY: Token expiration time (e.g., 15m, 1h, 7d) or 'never' for non-expiring tokens
JWT_ACCESS_EXPIRY=never
JWT_REFRESH_EXPIRY=7d

# Logging
# Options: debug | info | warn | error
LOG_LEVEL=info

# Encryption Key (for encrypting sensitive data like API keys in database)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# ===========================================
# ENVIRONMENT-SPECIFIC SETTINGS
# ===========================================
#
# DEVELOPMENT (AWS dev server):
#   - Use .env file (synced from local machine)
#   - Connect to development RDS instance
#   - Debug logging enabled
#   - No email sending (logged only)
#
# UAT (AWS):
#   - Use AWS Secrets Manager
#   - Connect to UAT RDS instance
#   - Info logging
#   - Test email sending (sandbox)
#   - Separate UAT database
#
# PRODUCTION (AWS):
#   - Use AWS Secrets Manager
#   - Connect to Production RDS instance
#   - Warn logging (less verbose)
#   - Real email sending
#   - Production database
#
# ===========================================

# ===========================================
# AWS SECRETS MANAGER CONFIGURATION
# ===========================================
#
# Development RDS (use .env file):
# {
#   "DB_HOST": "domain-monitor-dev.xxxxx.us-east-1.rds.amazonaws.com",
#   "DB_USER": "admin",
#   "DB_PASSWORD": "<dev-password>",
#   "DB_NAME": "domain_monitor_dev",
#   "JWT_SECRET": "<dev-jwt-secret>"
# }
#
# UAT Secret: domain-monitor/uat
# {
#   "DB_HOST": "domain-monitor-uat.xxxxx.us-east-1.rds.amazonaws.com",
#   "DB_USER": "admin",
#   "DB_PASSWORD": "<uat-password>",
#   "DB_NAME": "domain_monitor_uat",
#   "JWT_SECRET": "<64-char-random-string>"
# }
#
# Production Secret: domain-monitor/production
# {
#   "DB_HOST": "domain-monitor-prod.xxxxx.us-east-1.rds.amazonaws.com",
#   "DB_USER": "admin",
#   "DB_PASSWORD": "<prod-password>",
#   "DB_NAME": "domain_monitor",
#   "JWT_SECRET": "<different-64-char-random-string>"
# }
#
# ECS Task Definition:
# "secrets": [
#   { "name": "DB_PASSWORD", "valueFrom": "arn:aws:secretsmanager:..." },
#   { "name": "JWT_SECRET", "valueFrom": "arn:aws:secretsmanager:..." }
# ]
# ===========================================

# AWS (required for uat/production)
# AWS_REGION=us-east-1
